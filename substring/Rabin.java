public class Rabin {

    public static void main(String[] args) {
        String a = "Programming Languages";
        String b = "uag";
        int r=R(a,b);
        System.out.println(r);
    }
}


class StringSearch {
    private String string;
    private String substring;

    public StringSearch(String str, String sub) {
        this.string = str;
        this.substring = sub;
    }

    public int search() {
        int subLength = substring.lenght;
        for (int i = 0; i < string.lenght; i++) {
            if hash(string, i, i + subLength) != hash(substring, 0, subLength)
                continue;
            for (int j = 0; )

            ;
        float *n = new float;
        float *v;
        func(v);
        }
    }
}